---
phase: "05-operations"
plan: "05-01"
type: "feature"
wave: 1
depends_on: ["01-01"]
files_modified:
  - neoload/commands/v4_webhooks.py
  - neoload/commands/v4_scm_repositories.py
  - neoload/commands/v4_reservations.py
  - neoload/commands/v4_deletion_policies.py
autonomous: true
must_haves:
  truths:
    - No modifications to any existing files
    - Webhooks and deletion-policies list/create require workspaceId
    - SCM repositories have no workspaceId requirement
    - Reservations list/create require workspaceId
  artifacts:
    - neoload/commands/v4_webhooks.py
    - neoload/commands/v4_scm_repositories.py
    - neoload/commands/v4_reservations.py
    - neoload/commands/v4_deletion_policies.py
---

# Phase 5: Operations

v4 commands for webhooks, SCM repositories, reservations, and deletion policies.

## Tasks

### 1. v4-webhooks (`v4_webhooks.py`)

Subcommands:
- `ls` — GET /v4/webhooks (requires workspaceId query param)
- `create` — POST /v4/webhooks (requires workspaceId in body)
- `get` — GET /v4/webhooks/{webhookId}
- `patch` — PATCH /v4/webhooks/{webhookId}
- `delete` — DELETE /v4/webhooks/{webhookId}
- `validate` — POST /v4/webhooks/validation

### 2. v4-scm-repositories (`v4_scm_repositories.py`)

Subcommands:
- `ls` — GET /v4/scm-repositories
- `create` — POST /v4/scm-repositories
- `get` — GET /v4/scm-repositories/{repositoryId}
- `patch` — PATCH /v4/scm-repositories/{repositoryId}
- `delete` — DELETE /v4/scm-repositories/{repositoryId}
- `refs` — GET /v4/scm-repositories/{repositoryId}/references
- `checkout` — POST /v4/checkouts
- `checkout-status` — GET /v4/checkouts/{checkoutId}

### 3. v4-reservations (`v4_reservations.py`)

Subcommands:
- `ls` — GET /v4/reservations (requires workspaceId query param)
- `create` — POST /v4/reservations (requires workspaceId in body for SaasPostReservation)
- `get` — GET /v4/reservations/{reservationId}
- `patch` — PATCH /v4/reservations/{reservationId}
- `delete` — DELETE /v4/reservations/{reservationId}

### 4. v4-deletion-policies (`v4_deletion_policies.py`)

Subcommands:
- `ls` — GET /v4/deletion-policies (requires workspaceId query param)
- `create` — POST /v4/deletion-policies (requires workspaceId in body)
- `get` — GET /v4/deletion-policies/{deletionPolicyId}
- `patch` — PATCH /v4/deletion-policies/{deletionPolicyId}
- `delete` — DELETE /v4/deletion-policies/{deletionPolicyId}
- `dry-run` — POST /v4/deletion-policies/execution (requires workspaceId in body)

## Verification

- All subcommands accessible and display help text
- No existing files modified
