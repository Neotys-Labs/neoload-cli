---
phase: "08-users-identity"
plan: "08-01"
type: "feature"
wave: 1
depends_on: ["01-01"]
files_modified:
  - neoload/commands/v4_users.py
  - neoload/commands/v4_me.py
  - neoload/commands/v4_sessions.py
  - neoload/commands/v4_settings.py
  - neoload/commands/v4_sso.py
  - neoload/commands/v4_ldap.py
autonomous: true
must_haves:
  truths:
    - No modifications to any existing files
    - No workspaceId required on any user/identity endpoint
    - SSO/LDAP commands are admin operations (on-prem focused)
  artifacts:
    - neoload/commands/v4_users.py
    - neoload/commands/v4_me.py
    - neoload/commands/v4_sessions.py
    - neoload/commands/v4_settings.py
    - neoload/commands/v4_sso.py
    - neoload/commands/v4_ldap.py
---

# Phase 8: Users and Identity

v4 commands for user management, profile, sessions, account settings, SSO, and LDAP.

## Tasks

### 1. v4-users (`v4_users.py`)

Subcommands:
- `ls` — GET /v4/users
- `create` — POST /v4/users
- `get` — GET /v4/users/{userId}
- `patch` — PATCH /v4/users/{userId}
- `delete` — DELETE /v4/users/{userId}
- `workspaces-ls` — GET /v4/users/{userId}/workspaces
- `workspaces-add` — PUT /v4/users/{userId}/workspaces (requires workspaceId in body)
- `workspaces-remove` — DELETE /v4/users/{userId}/workspaces/{workspaceId}

### 2. v4-me (`v4_me.py`)

Subcommands:
- `get` — GET /v4/me
- `patch` — PATCH /v4/me
- `password` — PUT /v4/me/password
- `tokens-ls` — GET /v4/me/tokens
- `tokens-create` — POST /v4/me/tokens
- `tokens-delete` — DELETE /v4/me/tokens/{token}
- `features` — GET /v4/me/features

### 3. v4-sessions (`v4_sessions.py`)

Subcommands:
- `create` — POST /v4/sessions
- `delete` — DELETE /v4/sessions

### 4. v4-settings (`v4_settings.py`)

Subcommands:
- `get` — GET /v4/settings
- `patch` — PATCH /v4/settings
- `information` — GET /v4/information
- `subscription` — GET /v4/subscription

### 5. v4-sso (`v4_sso.py`)

Subcommands:
- `config-get` — GET /v4/sso/configuration
- `config-create` — POST /v4/sso/configuration
- `config-put` — PUT /v4/sso/configuration
- `config-patch` — PATCH /v4/sso/configuration
- `config-delete` — DELETE /v4/sso/configuration
- `config-status` — GET /v4/sso/configuration/status
- `saml-idp-get` — GET /v4/saml2/identity-provider-metadata
- `saml-idp-put` — PUT /v4/saml2/identity-provider-metadata
- `saml-idp-delete` — DELETE /v4/saml2/identity-provider-metadata
- `saml-sp-metadata` — GET /v4/saml2/service-provider-metadata

### 6. v4-ldap (`v4_ldap.py`)

Subcommands:
- `config-get` — GET /v4/ldap/configuration
- `config-patch` — PATCH /v4/ldap/configuration
- `entities-ls` — GET /v4/ldap/authorized-entities
- `entities-create` — POST /v4/ldap/authorized-entities
- `entities-patch` — PATCH /v4/ldap/authorized-entities/{authorizedEntityId}
- `entities-delete` — DELETE /v4/ldap/authorized-entities/{ldapAuthorizedEntityId}
- `search-users` — POST /v4/ldap/users/search
- `search-groups` — POST /v4/ldap/groups/search
- `search-user-groups` — POST /v4/ldap/users/{login}/groups/search

## Verification

- All subcommands accessible and display help text
- No existing files modified
