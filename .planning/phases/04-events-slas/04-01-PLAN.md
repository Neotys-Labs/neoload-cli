---
phase: "04-events-slas"
plan: "04-01"
type: "feature"
wave: 1
depends_on: ["02-01"]
files_modified:
  - neoload/commands/v4_events.py
  - neoload/commands/v4_slas.py
autonomous: true
must_haves:
  truths:
    - No modifications to any existing files
    - All endpoints scoped by resultId (no workspaceId needed)
  artifacts:
    - neoload/commands/v4_events.py
    - neoload/commands/v4_slas.py
---

# Phase 4: Events and SLAs

v4 commands for result events and SLA data.

## Tasks

### 1. v4-events (`v4_events.py`)

Endpoints: `/v4/results/{resultId}/events`, sub-resources

Subcommands:
- `ls` — GET /v4/results/{resultId}/events
- `create` — POST /v4/results/{resultId}/events
- `get` — GET /v4/results/{resultId}/events/{eventId}
- `patch` — PATCH /v4/results/{resultId}/events/{eventId}
- `delete` — DELETE /v4/results/{resultId}/events/{eventId}
- `errors` — GET /v4/results/{resultId}/events/errors
- `statistics` — GET /v4/results/{resultId}/events/statistics
- `content` — GET /v4/results/{resultId}/events/contents/{contentId}

Options: `--result-id` (required), `--event-id`, `--content-id`, `--file` for JSON body.

### 2. v4-slas (`v4_slas.py`)

Subcommands:
- `ls` — GET /v4/results/{resultId}/slas

Options: `--result-id` (required).

## Verification

- All subcommands accessible and display help text
- No existing files modified
